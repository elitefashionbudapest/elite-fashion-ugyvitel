<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Közvetlen hozzáférés tiltása az app mappákhoz
<IfModule mod_rewrite.c>
    RewriteRule ^(app|config|database|storage|vendor)/ - [F,L]
</IfModule>
